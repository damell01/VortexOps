name: One-time Whatnot post-switch 403

on:
  push:
    branches: [Dev]
    paths:
      - '.github/workflows/one-time-whatnot-post-switch-403.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Dev
          fetch-depth: 0
      - name: Patch post-switch 403 handling
        run: |
          python3 .github/scripts/patch-whatnot-post-switch-403.py
          node --check scripts/whatnot-scraper.cjs
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/whatnot-scraper.cjs
          git commit -m 'fix(scraper): avoid challenged post-switch toast redirect'
          git push origin HEAD:Dev

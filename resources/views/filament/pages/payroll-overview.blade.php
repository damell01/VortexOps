<x-filament-panels::page>
@php
    $current = $this->currentPayRun();
    $attention = $this->needsAttention();
    $breakdown = $this->currentBreakdown();
    $recent = $this->recentPayRuns();
    $shows = $this->currentWeekShows();
    $readiness = $this->readinessSummary();
    $workflow = $this->workflowBreakdown();
    $mock = $this->mockPayRun();
    $mockCalc = $this->mockShowCalculation();
    $activeWorkflow = request()->string('workflow')->toString() ?: 'all';
    $baseUrl = \App\Filament\Pages\PayrollOverview::getUrl();
@endphp
<style>
.vx-pay{max-width:1440px;margin:0 auto;display:grid;gap:14px}.vx-card{border:1px solid #e5e7eb;background:#fff;border-radius:16px;box-shadow:0 1px 2px rgba(15,23,42,.04)}.dark .vx-card{border-color:#263248;background:#101827}.vx-hero{padding:18px 20px}.vx-kpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));border-top:1px solid #eef0f3;margin-top:16px}.dark .vx-kpis{border-color:#263248}.vx-kpi{padding:14px 15px;border-right:1px solid #eef0f3;min-width:0}.dark .vx-kpi{border-color:#263248}.vx-kpi:last-child{border-right:0}.vx-kpi label{display:block;font-size:10px;text-transform:uppercase;letter-spacing:.05em;font-weight:800;color:#9ca3af}.vx-kpi strong{display:block;margin-top:4px;font-size:19px;color:#111827;white-space:nowrap}.dark .vx-kpi strong{color:#fff}.vx-actions{display:flex;flex-wrap:wrap;gap:8px}.vx-btn{display:inline-flex;align-items:center;justify-content:center;min-height:40px;border-radius:10px;padding:8px 12px;font-size:11px;font-weight:800;border:1px solid #d1d5db;color:#374151;background:#fff}.dark .vx-btn{border-color:#475569;color:#e5e7eb;background:#111827}.vx-btn.primary{background:#2563eb;border-color:#2563eb;color:#fff}.vx-grid{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(300px,.55fr);gap:14px}.vx-section{padding:18px}.vx-section h3{font-size:14px;font-weight:800;color:#111827}.dark .vx-section h3{color:#fff}.vx-sub{font-size:11px;color:#6b7280;margin-top:2px}.vx-alert{display:flex;gap:8px;border-radius:11px;background:#fff7ed;color:#9a3412;padding:9px 11px;font-size:11px;margin-top:7px}.dark .vx-alert{background:rgba(154,52,18,.16);color:#fdba74}.vx-ok{border-radius:11px;background:#ecfdf5;color:#047857;padding:10px 11px;font-size:11px;font-weight:750;margin-top:9px}.vx-filters{display:flex;gap:6px;overflow-x:auto;margin-top:12px}.vx-filter{display:inline-flex;gap:5px;align-items:center;white-space:nowrap;border:1px solid #e5e7eb;border-radius:999px;padding:6px 9px;font-size:10px;font-weight:800;color:#6b7280}.dark .vx-filter{border-color:#374151;color:#d1d5db}.vx-filter.active{background:#111827;color:#fff;border-color:#111827}.dark .vx-filter.active{background:#fff;color:#111827;border-color:#fff}.vx-show{display:grid;grid-template-columns:minmax(230px,1.25fr) 125px repeat(5,minmax(78px,.5fr)) 115px;gap:8px;align-items:center;padding:11px 12px;border-top:1px solid #f1f3f5;font-size:11px}.dark .vx-show{border-color:#1f2937}.vx-show.head{background:#f8fafc;border-top:0;font-size:9px;text-transform:uppercase;letter-spacing:.05em;font-weight:800;color:#94a3b8}.dark .vx-show.head{background:#1f2937}.vx-name{font-size:12px;font-weight:800;color:#111827;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.dark .vx-name{color:#fff}.vx-meta{font-size:10px;color:#6b7280;margin-top:2px}.vx-num{text-align:right;font-variant-numeric:tabular-nums}.vx-chip{display:inline-flex;border-radius:999px;padding:4px 7px;font-size:9px;font-weight:800;background:#f3f4f6;color:#4b5563}.dark .vx-chip{background:#1f2937;color:#d1d5db}.vx-chip.ready{background:#ecfdf5;color:#047857}.vx-chip.blocked{background:#fff7ed;color:#c2410c}.vx-chip.run{background:#eff6ff;color:#1d4ed8}.vx-link{display:inline-flex;align-items:center;justify-content:center;min-height:34px;border-radius:9px;border:1px solid #d1d5db;padding:6px 8px;font-size:10px;font-weight:800;color:#374151}.dark .vx-link{border-color:#475569;color:#e5e7eb}.vx-runrow{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:11px 0;border-top:1px solid #f1f3f5}.dark .vx-runrow{border-color:#1f2937}.vx-runrow strong{font-size:12px}.vx-runrow span{font-size:10px;color:#6b7280}.vx-totalbox{border-radius:14px;background:#eff6ff;padding:15px}.dark .vx-totalbox{background:#172554}.vx-totalbox strong{display:block;font-size:28px;color:#1d4ed8}.dark .vx-totalbox strong{color:#93c5fd}.vx-sim{border:1px dashed #a78bfa;background:#faf5ff}.dark .vx-sim{background:#211634;border-color:#7c3aed}.vx-sim-head{display:flex;justify-content:space-between;gap:12px;align-items:start}.vx-sim-badge{display:inline-flex;border-radius:999px;padding:4px 8px;font-size:9px;font-weight:800;background:#ede9fe;color:#6d28d9}.vx-sim-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:12px}.vx-sim-stat{padding:10px;border-radius:10px;background:rgba(255,255,255,.75)}.dark .vx-sim-stat{background:rgba(17,24,39,.65)}.vx-sim-stat label{display:block;font-size:9px;text-transform:uppercase;font-weight:800;color:#8b5cf6}.vx-sim-stat strong{display:block;margin-top:3px;font-size:18px}.vx-sim-row{display:grid;grid-template-columns:minmax(150px,1fr) 100px 90px 100px;gap:8px;padding:8px 0;border-top:1px solid rgba(139,92,246,.15);font-size:11px;align-items:center}.vx-sim-row.head{font-size:9px;text-transform:uppercase;font-weight:800;color:#8b5cf6}.vx-sim-note{font-size:10px;color:#6b7280;margin-top:8px}.vx-lab{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(320px,.75fr);gap:12px;margin-top:14px}.vx-lab-card{border:1px solid rgba(139,92,246,.2);border-radius:12px;padding:12px;background:rgba(255,255,255,.68)}.dark .vx-lab-card{background:rgba(17,24,39,.55)}.vx-input{width:100%;min-height:36px;border:1px solid #d1d5db;border-radius:8px;padding:6px 8px;font-size:11px;background:#fff}.dark .vx-input{background:#111827;border-color:#475569;color:#fff}.vx-readonly{width:100%;min-height:36px;border:1px solid rgba(139,92,246,.18);border-radius:8px;padding:7px 8px;font-size:11px;background:rgba(255,255,255,.55);color:#374151}.dark .vx-readonly{background:rgba(17,24,39,.5);color:#e5e7eb}.vx-lab-fields{display:grid;grid-template-columns:repeat(5,1fr);gap:8px}.vx-product-row{display:grid;grid-template-columns:minmax(160px,1fr) 90px 110px 110px 34px;gap:7px;align-items:end;padding:8px 0;border-top:1px solid rgba(139,92,246,.12)}.vx-label{display:block;font-size:8px;text-transform:uppercase;letter-spacing:.05em;font-weight:800;color:#8b5cf6;margin-bottom:3px}.vx-calc-row{display:flex;justify-content:space-between;gap:12px;padding:6px 0;border-top:1px solid rgba(139,92,246,.12);font-size:11px}.vx-calc-row strong{font-variant-numeric:tabular-nums}.vx-formula{margin-top:10px;padding:9px;border-radius:9px;background:#ede9fe;color:#5b21b6;font-size:10px;line-height:1.4}.dark .vx-formula{background:#2e1065;color:#ddd6fe}
@media(max-width:1000px){.vx-kpis{grid-template-columns:repeat(3,1fr)}.vx-grid,.vx-lab{grid-template-columns:1fr}.vx-show{grid-template-columns:1fr 1fr}.vx-show.head{display:none}.vx-show>div:first-child{grid-column:1/-1}.vx-show>div:nth-child(n+3):nth-child(-n+7){background:#f8fafc;border-radius:8px;padding:7px;text-align:left}.dark .vx-show>div:nth-child(n+3):nth-child(-n+7){background:#1f2937}.vx-show>div:last-child{grid-column:1/-1}.vx-link{width:100%;min-height:40px}.vx-sim-grid{grid-template-columns:1fr 1fr}.vx-lab-fields{grid-template-columns:1fr 1fr}}
@media(max-width:640px){.vx-pay{gap:10px}.vx-hero,.vx-section{padding:14px}.vx-kpis{grid-template-columns:1fr 1fr;margin-inline:-14px;margin-bottom:-14px}.vx-kpi:nth-child(2n){border-right:0}.vx-kpi strong{font-size:17px}.vx-actions{display:grid;grid-template-columns:1fr 1fr;width:100%}.vx-btn{min-height:44px}.vx-show{margin-top:8px;border:1px solid #e5e7eb;border-radius:12px;padding:11px}.dark .vx-show{border-color:#374151}.vx-show>div:nth-child(2){grid-column:1/-1}.vx-title{font-size:20px!important}.vx-sim-grid,.vx-lab-fields{grid-template-columns:1fr 1fr}.vx-sim-row{grid-template-columns:1fr 1fr}.vx-sim-row.head{display:none}.vx-sim-row>div:first-child{grid-column:1/-1}.vx-product-row{grid-template-columns:1fr 1fr}.vx-product-row>div:first-child{grid-column:1/-1}.vx-product-row>button{grid-column:1/-1;width:100%}}
</style>
<div class="vx-pay">
    <section class="vx-card vx-hero"><div class="flex flex-wrap items-start justify-between gap-4"><div><div class="text-xs font-bold uppercase tracking-wide text-primary-600">Current weekly payroll</div><h1 class="vx-title mt-1 text-2xl font-bold text-gray-950 dark:text-white">{{ ($current?->week_start ?? now()->startOfWeek())->format('M j') }} – {{ ($current?->week_end ?? now()->endOfWeek())->format('M j, Y') }}</h1><p class="mt-1 text-xs text-gray-500">Resolve show blockers, review calculated pay, then finalize one weekly run.</p></div><div class="vx-actions">@if($current)<a class="vx-btn primary" href="{{ \App\Filament\Resources\WeeklyPayoutBatchResource::getUrl('view',['record'=>$current]) }}">Review Current Pay Run</a>@else<a class="vx-btn primary" href="{{ \App\Filament\Resources\WeeklyPayoutBatchResource::getUrl('create') }}">Create Pay Run</a>@endif<a class="vx-btn" href="#mock-pay-run">Mock Pay Run</a><a class="vx-btn" href="{{ \App\Filament\Pages\PaymentStructures::getUrl() }}">Payment Structures</a><a class="vx-btn" href="{{ \App\Filament\Resources\WeeklyPayoutBatchResource::getUrl('index') }}">History</a></div></div><div class="vx-kpis"><div class="vx-kpi"><label>Payroll Total</label><strong>${{ number_format((float)($current?->total_payout ?? 0),2) }}</strong></div><div class="vx-kpi"><label>People</label><strong>{{ $breakdown['people'] }}</strong></div><div class="vx-kpi"><label>Streamer Pay</label><strong>${{ number_format($breakdown['streamer_total'],0) }}</strong></div><div class="vx-kpi"><label>Fulfillment Pay</label><strong>${{ number_format($breakdown['fulfillment_total'],0) }}</strong></div><div class="vx-kpi"><label>Ready / In Run</label><strong class="text-emerald-600">{{ $readiness['ready'] }}</strong></div><div class="vx-kpi"><label>Blocked</label><strong class="{{ $readiness['review'] ? 'text-amber-600' : 'text-emerald-600' }}">{{ $readiness['review'] }}</strong></div></div></section>

    <section id="mock-pay-run" class="vx-card vx-section vx-sim">
        <div class="vx-sim-head"><div><div class="vx-sim-badge">SIMULATION · REAL CATALOG COSTS · NO WRITES</div><h3 class="mt-2">Mock Pay Run & Show Calculator</h3><div class="vx-sub">Use actual products and their current catalog cost basis with simulated quantities and show numbers. This lets us compare the math to production without creating a show, deduction, payout or inventory movement.</div></div><div class="text-right text-xs font-semibold text-violet-700 dark:text-violet-300">{{ $mock['week_start']->format('M j') }} – {{ $mock['week_end']->format('M j') }}</div></div>
        <div class="vx-sim-grid"><div class="vx-sim-stat"><label>Projected Payroll</label><strong>${{ number_format($mock['total'],2) }}</strong></div><div class="vx-sim-stat"><label>People</label><strong>{{ $mock['people'] }}</strong></div><div class="vx-sim-stat"><label>Streamer Pay</label><strong>${{ number_format($mock['streamer_total'],2) }}</strong></div><div class="vx-sim-stat"><label>Fulfillment Pay</label><strong>${{ number_format($mock['fulfillment_total'],2) }}</strong></div></div>
        <div class="mt-3"><div class="vx-sim-row head"><div>Person</div><div>Type</div><div>Shows</div><div class="vx-num">Projected</div></div>@forelse($mock['rows'] as $row)<div class="vx-sim-row"><div><div class="font-semibold">{{ $row['name'] }}</div><div class="vx-meta">{{ \App\Models\Streamer::payoutTypeLabels()[$row['payout_type']] ?? $row['payout_type'] }} · {{ $row['entries'] }} payout entr{{ $row['entries']===1?'y':'ies' }}</div></div><div><span class="vx-chip {{ $row['member_type']==='fulfillment'?'run':'ready' }}">{{ ucfirst($row['member_type']) }}</span></div><div>{{ $row['shows'] }}</div><div class="vx-num font-bold">${{ number_format($row['amount'],2) }}</div></div>@empty<div class="py-6 text-center text-xs text-gray-500">No calculated payouts exist for this week yet.</div>@endforelse</div>

        <div class="vx-lab">
            <div class="vx-lab-card">
                <div class="flex items-start justify-between gap-3"><div><h3>Simulated Show Inputs</h3><div class="vx-sub">Products and costs come from the real catalog. Only quantities and show-level numbers are simulated.</div></div><button type="button" wire:click="addMockProduct" class="vx-btn">+ Catalog Product</button></div>
                <div class="vx-lab-fields mt-3"><div><label class="vx-label">Gross Revenue</label><input type="number" step="0.01" min="0" class="vx-input" wire:model.live.debounce.350ms="mockGrossRevenue"></div><div><label class="vx-label">Hours</label><input type="number" step="0.25" min="0" class="vx-input" wire:model.live.debounce.350ms="mockHours"></div><div><label class="vx-label">Shipments</label><input type="number" min="0" class="vx-input" wire:model.live.debounce.350ms="mockShipments"></div><div><label class="vx-label">Profit Share %</label><input type="number" step="0.01" min="0" class="vx-input" wire:model.live.debounce.350ms="mockPercentage"></div><div><label class="vx-label">Tips Added</label><input type="number" step="0.01" min="0" class="vx-input" wire:model.live.debounce.350ms="mockTips"></div></div>
                <div class="mt-3"><div class="vx-product-row"><div class="vx-label">Real Product</div><div class="vx-label">Qty</div><div class="vx-label">Catalog Cost</div><div class="vx-label">Line Cost</div><div></div></div>@foreach($mockProducts as $i=>$product)<div class="vx-product-row" wire:key="mock-product-{{ $product['product_id'] ?? $i }}"><div class="vx-readonly"><div class="font-semibold">{{ $product['name'] ?? 'Product' }}</div><div class="text-[9px] text-gray-500">{{ $product['sku'] ?? 'No SKU' }}</div></div><div><input type="number" min="0" step="1" class="vx-input" wire:model.live.debounce.350ms="mockProducts.{{ $i }}.quantity"></div><div class="vx-readonly flex items-center font-semibold">${{ number_format((float)($mockCalc['products'][$i]['unit_cost'] ?? $product['unit_cost'] ?? 0),2) }}</div><div class="vx-readonly flex items-center font-semibold">${{ number_format((float)($mockCalc['products'][$i]['line_total'] ?? 0),2) }}</div><button type="button" wire:click="removeMockProduct({{ $i }})" class="vx-btn">×</button></div>@endforeach</div>
            </div>
            <div class="vx-lab-card">
                <h3>Calculation Breakdown</h3><div class="vx-sub">Same ProfitShareFormula used by payroll for profit-share working.</div>
                <div class="mt-3"><div class="vx-calc-row"><span>Gross revenue</span><strong>${{ number_format($mockCalc['gross_revenue'],2) }}</strong></div><div class="vx-calc-row"><span>Real catalog product cost</span><strong>− ${{ number_format($mockCalc['product_cost'],2) }}</strong></div><div class="vx-calc-row"><span>Burden</span><strong>− ${{ number_format($mockCalc['burden'],2) }}</strong></div><div class="vx-calc-row"><span>Net revenue basis</span><strong>${{ number_format($mockCalc['net_revenue'],2) }}</strong></div><div class="vx-calc-row"><span>Profit share ({{ number_format($mockCalc['percentage'],2) }}%)</span><strong>${{ number_format($mockCalc['base_share'],2) }}</strong></div><div class="vx-calc-row"><span>Tips added</span><strong>+ ${{ number_format($mockCalc['tips'],2) }}</strong></div><div class="vx-calc-row text-base"><span class="font-bold">Projected payout</span><strong>${{ number_format($mockCalc['projected_payout'],2) }}</strong></div><div class="vx-calc-row"><span>Business after product, burden & payout</span><strong class="{{ $mockCalc['business_after_payroll'] < 0 ? 'text-red-600' : 'text-emerald-600' }}">${{ number_format($mockCalc['business_after_payroll'],2) }}</strong></div></div>
                <div class="vx-formula">{{ $mockCalc['explanation'] }} @if($mockCalc['tips'] > 0) Then + ${{ number_format($mockCalc['tips'],2) }} simulated tips = ${{ number_format($mockCalc['projected_payout'],2) }} projected payout.@endif</div>
                <div class="vx-sim-note">Product cost is re-read from each product's production cost basis (weighted average when available, otherwise unit cost). Burden rates come from the same saved payroll settings as production.</div>
            </div>
        </div>
        <div class="vx-sim-note">This section reads real product data but does not write to it: no WeeklyPayoutBatch, payout, show, product, deduction, shipment, or inventory movement is created or changed.</div>
    </section>

    <div class="vx-grid"><section class="vx-card vx-section"><div class="flex items-start justify-between gap-3"><div><h3>Shows in This Pay Period</h3><div class="vx-sub">Fix the workflow blocker from here instead of hunting through separate pages.</div></div><a href="{{ \App\Filament\Resources\ShowResource::getUrl('index') }}" class="text-xs font-semibold text-primary-600">All Shows →</a></div><div class="vx-filters">@foreach(['all'=>['All',$workflow['all']],'blocked'=>['Blocked',$workflow['blocked']],'ready'=>['Ready',$workflow['ready']],'in_run'=>['In Run',$workflow['in_run']],'paid'=>['Paid',$workflow['paid']]] as $key=>[$label,$count])<a class="vx-filter {{ $activeWorkflow===$key?'active':'' }}" href="{{ $key==='all'?$baseUrl:$baseUrl.'?workflow='.$key }}">{{ $label }} <span>{{ $count }}</span></a>@endforeach</div><div class="mt-3 overflow-hidden rounded-xl border border-gray-200 dark:border-gray-700"><div class="vx-show head"><div>Show</div><div>Status</div><div class="vx-num">Sales</div><div class="vx-num">Whatnot Net</div><div class="vx-num">COGS</div><div class="vx-num">Payroll</div><div class="vx-num">Show Net</div><div>Action</div></div>@forelse($shows as $show)@php $state=$show->getAttribute('workflow_state'); $pnl=$show->getAttribute('pnl_summary'); $probs=$show->getAttribute('payrun_problems')??[]; $resolution=$this->showResolution($show); $key=$state['key']??''; $tone=$probs!==[]?'blocked':($key==='paid'?'ready':($key==='payroll'?'run':($key==='payroll_ready'?'ready':''))); @endphp<div class="vx-show"><a href="{{ \App\Filament\Resources\ShowResource::getUrl('view',['record'=>$show]) }}" class="min-w-0"><div class="vx-name">{{ $show->title ?: 'Show #'.$show->id }}</div><div class="vx-meta">{{ $show->show_date?->format('M j') }} · {{ $show->streamers->pluck('name')->join(', ') ?: 'No streamer assigned' }}@if($probs!==[]) · {{ count($probs) }} blocker(s)@endif</div></a><div><span class="vx-chip {{ $tone }}">{{ $state['label'] ?? ucfirst(str_replace('_',' ',$key)) }}</span></div><div class="vx-num">${{ number_format((float)($pnl['gross']??0),0) }}</div><div class="vx-num">${{ number_format((float)($pnl['net']??0),0) }}</div><div class="vx-num">${{ number_format((float)($pnl['cogs']??0),0) }}</div><div class="vx-num">${{ number_format((float)($pnl['payouts']??0),0) }}</div><div class="vx-num font-bold {{ ($pnl['margin']??0)<0?'text-red-600':'text-emerald-600' }}">${{ number_format((float)($pnl['margin']??0),0) }}</div><div><a class="vx-link" href="{{ $resolution['url'] }}">{{ $resolution['label'] }}</a></div></div>@empty<div class="p-8 text-center text-sm text-gray-500">No shows in this pay period.</div>@endforelse</div></section><aside class="space-y-3"><section class="vx-card vx-section"><h3>Run Readiness</h3><div class="vx-sub">A draft should only be finalized when there are no blockers.</div>@forelse($attention as $warning)<div class="vx-alert"><strong>!</strong><span>{{ $warning }}</span></div>@empty<div class="vx-ok">✓ Current week is clear of payroll readiness issues.</div>@endforelse</section><section class="vx-card vx-section"><h3>Current Run</h3><div class="vx-sub">At-a-glance weekly total and next action.</div><div class="vx-totalbox mt-3"><span class="text-[10px] font-bold uppercase tracking-wide text-blue-600 dark:text-blue-300">Total payroll</span><strong>${{ number_format((float)($current?->total_payout ?? 0),2) }}</strong><div class="mt-1 text-xs text-blue-700 dark:text-blue-200">{{ $current ? (\App\Models\WeeklyPayoutBatch::statusLabels()[$current->status] ?? ucfirst($current->status)) : 'No run created' }}</div></div>@if($current)<a class="vx-btn primary mt-3 w-full" href="{{ \App\Filament\Resources\WeeklyPayoutBatchResource::getUrl('view',['record'=>$current]) }}">Open Current Pay Run</a>@endif</section><section class="vx-card vx-section"><h3>Recent Pay Runs</h3>@forelse($recent as $run)<a href="{{ \App\Filament\Resources\WeeklyPayoutBatchResource::getUrl('view',['record'=>$run]) }}" class="vx-runrow"><div><strong>{{ $run->week_start?->format('M j') }} – {{ $run->week_end?->format('M j') }}</strong><br><span>{{ \App\Models\WeeklyPayoutBatch::statusLabels()[$run->status] ?? ucfirst($run->status) }} · {{ $run->payouts_count }} entries</span></div><strong>${{ number_format((float)$run->total_payout,2) }}</strong></a>@empty<div class="py-5 text-center text-xs text-gray-500">No prior pay runs.</div>@endforelse</section></aside></div>
</div>
</x-filament-panels::page>